import { TutoViewchildDirective } from './tuto-viewchild.directive';

describe('TutoViewchildDirective', () => {
  it('should create an instance', () => {
    const directive = new TutoViewchildDirective();
    expect(directive).toBeTruthy();
  });
});
